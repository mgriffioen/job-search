{
  "name": "job-search",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Aggregates remote editorial, proofreading and content QA postings whose titles match Emily Griffioen's search terms.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch": "node scripts/fetch-jobs.mjs",
    "serve": "node scripts/stamp-assets.mjs && node scripts/serve.mjs",
    "start": "node scripts/fetch-jobs.mjs && node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "stamp": "node scripts/stamp-assets.mjs"
  },
  "license": "UNLICENSED"
}
